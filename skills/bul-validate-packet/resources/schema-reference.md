# Packet Schema Reference Placeholder

Later stories define handwritten validation rules for `bul.feature-packet.v1`. The validator must report packet validity separately from BMAD readiness and must not mutate inputs.
