# Receipt Verification Rules Placeholder

Future receipt verification will compare claimed writes and non-effects against run metadata and observed changed files. Verification fails closed on forbidden writes, missing metadata, and contradictory claims.
